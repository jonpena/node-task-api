import { Request, Response, Router } from 'express';

const router = Router();

/**
 * htpp://localhost:3002/items [GET]
 */
// router.get('/', (req: Request, res: Response) => {
// 	res.send({ data: 'aqui van nuestros modelos' });
// });

export { router };
